name: s3-simple-sdk
runtime:
  name: python
  options:
    virtualenv: venv
description: A python project for s3 to create a bucket
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
